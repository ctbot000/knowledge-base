# Index

One line per entry. Scan this first; open only what is relevant.

Adding an entry? Read [CONVENTIONS.md](CONVENTIONS.md) — the generality bar is
strict and this repository is public.

## agents

- [An agent knowledge base is retrieval-limited, not storage-limited](entries/agents/knowledge-base-retrieval-limits.md) — What gets read back is the constraint, so the index line matters more than the entry body. `knowledge-base`, `context`, `retrieval`
- [A model's "supports tools" flag does not guarantee structured tool calls](entries/agents/tool-support-flag-is-not-a-guarantee.md) — Small models emit the call as plain text; the server reports no call and finish_reason "stop", and the agent silently stops. `llm`, `tool-calling`, `local-inference`
- [A prompt keyword that unlocks an agent mode does not fire on scheduled input](entries/agents/keyword-opt-in-ignores-automated-input.md) — Scheduler and webhook input classifies as non-human, so the opt-in is silently skipped; enable the mode in settings the run reads. `agents`, `automation`, `scheduling`

## languages

- [GLSL rejects a long list of words it does not implement, and `half` is one of them](entries/languages/glsl-reserved-words.md) — Reserved-for-future-use identifiers are compile errors, and `half` is the natural name for a Blinn-Phong halfway vector. `glsl`, `webgl`, `shaders`
- [Node's strict assertions fail -0 against 0](entries/languages/assert-strict-rejects-negative-zero.md) — `assert.strictEqual` compares with `Object.is`, so a sign-flipping function returning `-0` fails a zero check that `===` would pass. `javascript`, `testing`, `numerics`
- [asyncio's Server.wait_closed() waits for live connections, not just the listener](entries/languages/asyncio-wait-closed-waits-for-connections.md) — Since Python 3.12 it blocks until every handler ends, so awaiting it before telling clients to leave deadlocks. `python`, `asyncio`, `shutdown`
- [Math.max cannot floor a NaN, so a clamped loop bound can still run zero times](entries/languages/math-max-cannot-floor-nan.md) — `Math.max(1, NaN)` is NaN, so the loop runs zero times while the `!== 0` liveness test keeps reporting the entity as moving. `javascript`, `numerics`, `simulation`

## practices

- [Cron cannot express an every-N-hours interval unless N divides 24](entries/practices/cron-interval-must-divide-period.md) — `*/5` on hours wraps to a 4-hour gap at midnight; use an interval timer or a self-rescheduling one-shot instead. `cron`, `scheduling`, `automation`
- [A regenerate-and-commit job must produce output that is a pure function of its input](entries/practices/generated-output-must-be-a-pure-function.md) — A build timestamp in the generated file makes every scheduled run commit, burying the one run that changed something. `automation`, `ci`, `code-generation`
- [A CLI that reads piped stdin hangs when its parent leaves the pipe open](entries/practices/cli-stdin-read-hangs.md) — `isatty` says "not a terminal", not "has data"; a pipe with no writer never reaches EOF. `cli`, `stdin`, `subprocess`
- [An interactive REPL needs one long-lived input reader, not one per prompt](entries/practices/repl-single-input-reader.md) — A reader opened per prompt drops type-ahead and every line of a paste after the first. `cli`, `repl`, `terminal`
- [An HTTP upgrade hands over bytes already read past the handshake](entries/practices/http-upgrade-head-buffer.md) — The `head` buffer holds the new protocol's first frames; ignore it and a server's greeting vanishes on loopback. `http`, `websocket`, `protocols`
- [A role=grid whose cells are not inside row elements is dropped from the accessibility tree](entries/practices/aria-grid-needs-row-elements.md) — Browsers prune the cells; the DOM still has every aria-label, so it reads as missing labels rather than invalid structure. `accessibility`, `aria`, `css`
- [An author display rule silently overrides the HTML hidden attribute](entries/practices/hidden-attribute-vs-display-rule.md) — `[hidden]` is only a UA stylesheet rule, so any class setting `display` keeps the element painted while `el.hidden` reads true. `css`, `html`, `dom`
- [One key press runs two steps when a bubbling handler reads state an inner handler just changed](entries/practices/key-event-runs-two-steps.md) — The second step is either another listener or a focused button's default action; `stopPropagation` fixes one and `preventDefault` the other. `dom`, `events`, `keyboard`
- [An animation lifetime counted in frames never expires while rAF is throttled](entries/practices/animation-lifetime-in-frames.md) — A hidden tab stops delivering frames, so a `life--` counter stalls and the overlay freezes on screen; end effects on wall-clock time. `animation`, `browser`, `requestAnimationFrame`
- [UI synced only from the animation loop cannot render the state change that stopped the loop](entries/practices/ui-state-synced-only-in-raf.md) — A visibilitychange handler that pauses and shows its panel from the frame loop never paints it; the model reads `paused` while the DOM still says `playing`. `browser`, `requestAnimationFrame`, `ui`
- [Speech synthesis picks a voice by locale, not by the language of the text](entries/practices/speech-synthesis-voice-follows-locale.md) — English text is read by the system-locale voice unless you set `lang` and pick a voice, and `getVoices()` is empty until `voiceschanged`. `web-speech`, `i18n`, `browser`
- [A speech utterance that never starts also never ends, while speechSynthesis.speaking reads true](entries/practices/speech-utterance-can-fire-no-events.md) — Blocked audio fires no `start`, `end` or `error` at all, so a promise awaiting `onend` deadlocks while both health probes look healthy. `web-speech`, `browser`, `async`
- [A swallowed storage write needs an in-memory copy, or the next read contradicts it](entries/practices/swallowed-storage-write-stale-read.md) — `try/catch` around `localStorage` hides the failure, not the consequence; re-reading the key returns the old value and the session contradicts itself. `storage`, `caching`, `error-handling`
- [A bare text node in a flex container becomes its own flex item](entries/practices/flex-container-wraps-bare-text-nodes.md) — It is wrapped in an anonymous flex item, so a label and its unit span land on separate rows and every item count is off by one. `css`, `flexbox`, `layout`
- [Blockification applies to a flex or grid item, not to anything inside it](entries/practices/blockification-stops-at-the-item.md) — The item gets a real box, but an inline wrapper inside it stays inline and collapses to 0x0, taking its absolutely positioned children with it. `css`, `layout`, `flexbox`
- [An inline SVG sizes itself from its viewBox unless both axes are definite](entries/practices/svg-intrinsic-size-beats-the-box.md) — A `viewBox` ratio blows up `1fr` grid tracks and beats `position:absolute` insets, so the graphic overflows and the next row paints over it. `css`, `svg`, `layout`
- [A grouping property silently turns transform-style preserve-3d into flat](entries/practices/grouping-property-flattens-preserve-3d.md) — `opacity`, `filter`, `clip-path` or a clipping `overflow` collapse 3D children into one plane while `getComputedStyle` still reports `preserve-3d`. `css`, `3d`, `transforms`
- [A 3D detail placed at a 2D-looking offset ends up inside the mesh and vanishes silently](entries/practices/3d-features-sink-into-the-surface.md) — There is no draw order to lean on; the part is added, lit and drawn, just behind an opaque surface, so it reads as a missing mesh. `3d`, `graphics`, `webgl`
- [A node mirrored with a negative scale also mirrors every rotation applied beneath it](entries/practices/mirrored-scale-inverts-rotation.md) — One value driving both halves of a symmetric rig moves them apart; the rig looks right at rest and breaks on the first animated frame. `3d`, `animation`, `scene-graph`
- [In a perspective view, a world-space proximity test rejects the target the player is aiming at](entries/practices/perspective-aim-needs-a-cone.md) — Aim is a ray, so a sphere around the cursor only works at the cursor's depth; test angular distance, and resolve to one best-aimed candidate. `3d`, `game-design`, `input`
- [A cooldown that elapses while its precondition is false fires the instant the precondition becomes true](entries/practices/cooldown-fires-on-precondition.md) — The agent acts on the first tick of acquiring the thing, so the interval paces nothing; symptoms land in physics, not in the timer. `simulation`, `game-design`, `timers`
- [A simulation that restores an identical state after each event replays the same episode, whatever the seed](entries/practices/deterministic-reset-replays-the-episode.md) — "The seed changes nothing" points at the generator; the repetition is in the reset upstream of it. `simulation`, `testing`, `randomness`
- [A raised-cosine contour is flat at its own rim, so contours sum into a height field with no creases](entries/practices/raised-cosine-contours-compose-without-seams.md) — Zero value and zero slope at the rim means primitives add with no crease and no impulse, which is what lets a body stay glued to the surface. `graphics`, `terrain`, `simulation`
- [Scattering points with `i * stride % size` lays them on a lattice, not at random](entries/practices/modular-stride-scatter-is-a-lattice.md) — Consecutive samples step by a constant, so "random" speckle rules itself into diagonals as density rises; use an R2 low-discrepancy sequence. `graphics`, `procedural-generation`, `randomness`
- [A no-guess puzzle board is found by rejection sampling against a solver that only makes the player's deductions](entries/practices/no-guess-puzzle-generation.md) — Keep a random board only if a deliberately weak solver clears it; cheap enough to draw on demand, but acceptance collapses as density rises. `algorithms`, `puzzles`, `game-design`
- [A wrongly wound triangle is not drawn inside out, it is not drawn at all](entries/practices/backface-culling-deletes-geometry.md) — Back-face culling discards it silently, so a missing highlight reads as a state bug rather than a geometry one; assert winding where meshes are built. `graphics`, `webgl`, `3d`
- [Aborting a make/unmake search by throwing skips every pending unmake](entries/practices/search-abort-skips-unmake.md) — The stack unwinds past every undo, leaving the shared position as deep as the search reached; it only shows up once the object is reused. `algorithms`, `search`, `error-handling`
- [Electron's findInPage findNext option starts a search session, it does not step to the next match](entries/practices/findinpage-findnext-starts-a-session.md) — `findNext: false` on the first request is dropped silently and `found-in-page` never fires; omitting the option starts a session. `electron`, `browser`, `find-in-page`
- [A shrinkable flex item contributes its content width, not its flex-basis, to an auto-sized container](entries/practices/flex-basis-does-not-size-an-auto-container.md) — `flex: 0 1 190px` renders at the text's width; use `width` for a fixed preferred size. `css`, `flexbox`, `layout`
- [Closing a socket with unread data sends RST, which can destroy what you just wrote](entries/practices/close-with-unread-data-sends-rst.md) — The peer sees ECONNRESET instead of the error you wrote; half-close and drain before closing. `tcp`, `sockets`, `networking`
- [One ACTIVITY_PAUSED does not mean the app left the foreground](entries/practices/activity-paused-is-not-app-backgrounded.md) — Usage events report activities, not apps; closing the session on the first pause silently under-counts foreground time. `android`, `usage-stats`, `instrumentation`
- [checkSelfPermission(POST_NOTIFICATIONS) answers DENIED on Android below 13](entries/practices/post-notifications-denied-below-api-33.md) — The permission does not exist pre-33, so the obvious guard mutes every notification on Android 10-12. `android`, `permissions`, `notifications`
- [Overlapping translucent shapes compound per fill(), not per shape](entries/practices/canvas-translucent-fill-compounding.md) — One path fills the union at a flat alpha; a fill per shape stacks at every overlap, and winding is not what causes it. `canvas`, `graphics`, `frontend`
- [A stroke on a union path redraws the seams that a single fill erased](entries/practices/stroke-outlines-every-subpath.md) — `stroke()` outlines every subpath, including the ones buried inside the union, so a rim added to a flat-filled blob reinstates its internal edges. `canvas`, `graphics`, `frontend`
- [Brightening a lamp cannot widen the pool of light on a wall of coplanar faces](entries/practices/lamp-power-cannot-widen-the-pool.md) — N·L fixes the lit radius by geometry, so more power only clips the centre; move the light off the surface and wrap the diffuse term. `graphics`, `lighting`, `webgl`
- [A metal surface lit only by punctual lights is black everywhere except its highlight](entries/practices/metal-needs-an-environment.md) — Metals have no diffuse term, so adding or brightening lamps cannot fill them; a prefiltered environment is the missing term. `graphics`, `rendering`, `pbr`
- [A colour palette is calibrated to its shading model, not to the screen](entries/practices/palette-is-calibrated-to-its-shading-model.md) — Flat fills are final values; as albedo they get multiplied down by ambient, N·L and fog, and the dark end vanishes first. `graphics`, `color`, `rendering`
- [A layout measured while the surface is hidden is zero, and rescaling from it cannot recover](entries/practices/zero-measurement-poisons-derived-layout.md) — A collapsed pane returns a 0x0 box; clamp to a floor and regenerate disposable state instead of scaling noise back up. `css`, `layout`, `canvas`
- [Two shapes sharing an edge must trace one curve, and only a self-symmetric one reverses by a sign flip](entries/practices/shared-edge-needs-a-self-symmetric-curve.md) — Asymmetry breaks the reverse traversal, leaving a hairline gap that looks like a placement bug. `geometry`, `graphics`, `procedural-generation`
- [An overlay on a letterboxed stage must size in container units, not viewport units](entries/practices/overlay-units-follow-the-stage.md) — `vw`/`vh` measure a box the stage does not occupy, so a phone-sized HUD swallows a fixed-aspect canvas. `css`, `layout`, `responsive`
- [A tug-of-war skill meter with no clock is always won by the patient player](entries/practices/skill-meter-needs-escalation.md) — Constant gain-vs-drain rates make skill decide the length, not the outcome; escalate the drain instead of adding a timer. `game-design`, `simulation`, `balance`
- [A moving target that outruns the thing chasing it fails a perfect player, not a bad one](entries/practices/tracking-target-vs-slew-rate.md) — Peak target speed above the controlled variable's slew rate makes a pursuit task unreachable; it reads as brutal tuning, so assert the ratio. `game-design`, `control`, `balance`
- [Simulated player error must be sampled per decision, not per tick](entries/practices/simulated-error-is-per-decision.md) — A per-tick miss chance is an 8ms jitter, not a handicap; survival stays flat and a punishing game reads as trivial. `game-design`, `simulation`, `testing`
- [An affine per-unit reward barely separates skill levels; a compounding one does](entries/practices/multiplicative-bonus-separates-skill.md) — Perfect and good play pay within 12% of each other, so run length stops reporting skill; a streak multiplier doubled the gap. `game-design`, `balance`, `simulation`
- [A level solver guided by straight-line distance reports maze levels as impossible](entries/practices/level-validation-needs-a-geodesic-heuristic.md) — Euclidean ranking never leaves a spiral's outer arm; rank on a flood-filled distance field, and check reachability and completability separately. `game-design`, `level-design`, `testing`
- [Where power comes from the player's motion, an interception AI that arrives early and stops is useless](entries/practices/interception-ai-must-arrive-moving.md) — A perfectly positioned stationary body returns less than it received, and the rally is lost three touches later, so the symptom points away from the movement policy. `game-design`, `ai`, `simulation`
- [A boundary clamp applied during integration must run again after collision resolution](entries/practices/boundary-clamp-runs-after-collision.md) — Collision resolution pushes a body straight through the wall the clamp just enforced; both pieces of code are correct and the order is not. `game-design`, `collision`, `simulation`
- [A capsule end that stops flush with the wall it joins leaves a notch that traps a body for good](entries/practices/capsule-junction-stubs-are-traps.md) — Every individual resolution is correct; sweep release positions across the whole world to find the shelf. `game-design`, `collision`, `geometry`
- [Scaling tangential velocity by a fixed factor per contact bleeds a rolling body to a stop](entries/practices/friction-must-scale-with-normal-impulse.md) — A resting contact is re-resolved every substep, so flat damping compounds; cap the tangential impulse by the normal one. `simulation`, `physics`, `game-design`
- [A flex item that grows ignores aspect-ratio, because both of its axes are then definite](entries/practices/aspect-ratio-loses-to-flex-grow.md) — `flex-grow` resolves the main size and `stretch` the cross size, so the declared ratio is inert and a canvas renders stretched. `css`, `flexbox`, `layout`
- [An obstacle that forces one input must fit inside the band where only that input works](entries/practices/forced-input-obstacle-fits-the-band.md) — The floor is the gap between standing and ducking hitboxes; the ceiling is every chained verb, so a barrier above a single jump is still beaten by a double one. `game-design`, `collision`, `level-design`
- [Nearest-neighbour blitting at a fractional offset seams every tile edge](entries/practices/fractional-blit-offset-seams.md) — `imageSmoothingEnabled = false` is nearest-neighbour, not exact; a fractional camera offset drops a row at every tile edge, and only while it moves. `canvas`, `graphics`, `frontend`
- [A balance change that does not move the outcome means the dominant term is elsewhere](entries/practices/balance-knob-with-no-effect.md) — an inert tuning knob is a fact about the model; attribute the resource by cause before changing the number again. `game-design`, `balance`, `simulation`
- [A reflecting boundary turns a projectile game's overshoot into a second attempt at the target](entries/practices/reflecting-boundary-rescues-overshoot.md) — A ceiling above the target makes full power always score; the tell is a scoring window whose upper edge equals the input cap. `game-design`, `collision`, `balance`
- [A centre-aimed grid action deadlocks for an entity narrower than a cell](entries/practices/centre-aimed-action-misses-the-support.md) — Collision uses the whole footprint but the action uses one ray, so a straddling entity drills at the empty tile beside the one holding it up. `game-design`, `collision`, `simulation`
- [An extraction economy must be net-positive with the starting loadout](entries/practices/extraction-economy-must-pay-at-tier-zero.md) — A trip that costs more than it earns reads as a stuck agent, not as a price; measure yield along the path travelled, not map density. `game-design`, `balance`, `simulation`
- [A lifecycle autosave persists the placeholder state behind the menu](entries/practices/autosave-persists-placeholder-state.md) — beforeunload fires for the demo world under the title card, so a first-time visitor is offered "Continue" for a session they never began. `persistence`, `ui`, `browser`
- [Fitting text into a narrowing shape needs a search over wrap width, not just font size](entries/practices/fitting-text-to-a-shape.md) — Shrinking the font wraps the text into fewer, longer lines, so the single-variable loop bottoms out still overflowing. `css`, `typography`, `layout`
- [A perfectly regular packing passes an impulse straight through instead of scattering](entries/practices/perfect-packing-transmits-impulse.md) — Sequential pair collisions on an exact lattice act as a Newton's cradle: one body leaves fast, the rest sit still, and every collision is individually correct. `simulation`, `physics`, `game-design`
- [Framing a box in a perspective camera is a per-corner solve, not a bounding sphere](entries/practices/framing-a-box-is-a-per-corner-solve.md) — A sphere fit is orientation-blind and empties a portrait frame; projecting extents instead clips the near corners. `3d`, `camera`, `layout`
- [A camera solved to fill the frame converges on a face-on view and throws the perspective away](entries/practices/auto-framing-collapses-to-face-on.md) — Projected area peaks looking down a planar subject's normal, so the fill objective is what flattens the picture; clamp the angle range. `3d`, `camera`, `game-design`
- [A collision world's segment list is not a display representation, and drawing it one primitive per segment shows the joins](entries/practices/segment-list-needs-rechaining-to-draw.md) — Rounded ends poke through their neighbours and a curve renders ribbed; chain the segments into polylines first. `graphics`, `game-design`, `geometry`
- [A steeper entry angle widens the target, so the best shot is the one that leaves the frame](entries/practices/steep-entry-widens-the-target.md) — Effective opening scales with the entry angle's sine, so the widest-window trajectory is also the tallest; measure apexes, not just makes. `game-design`, `balance`, `camera`
- [In a chained-reflection aiming problem, most solutions are knife-edge, so rank candidates by tolerance rather than by success](entries/practices/rank-solutions-by-tolerance.md) — Median scoring window went 0.04° to 0.58° by re-ranking the same candidates; a pass/fail verdict cannot tell a robust shot from a fluke. `game-design`, `search`, `simulation`
- [A hidden page's timer budget turns setTimeout into a one-second yield, but only when there is work between yields](entries/practices/hidden-page-timer-budget.md) — Chunked work stalls off-screen while a microbenchmark of the yield reports 0 ms; MessageChannel is not budgeted. `browser`, `performance`, `async`
- [A simulated player that waits on its target absorbs everything that arrives there first](entries/practices/bot-camping-on-target-inflates-difficulty.md) — A controller with zero injected error still making the game's own mistakes is a policy bug, and its cost gets charged to the level design. `game-design`, `simulation`, `testing`
- [A clock topped up per unit of progress escalates on its own, and states its own survivability](entries/practices/progress-topup-clock-escalates-monotonically.md) — Buffer `M`, top-up `k`, length `L`: survivable while pace stays under `k + M/L` of optimal, so the whole curve is one monotone sequence. `game-design`, `balance`, `simulation`
- [Sweep the action space before tuning difficulty — "hard" and "impossible" look identical from inside the game](entries/practices/measure-solution-density-before-tuning.md) — Brute-force the aim/power space from real positions: near-zero solution density is a physics fault no amount of search or aim assist can fix. `game-design`, `simulation`, `testing`
- [A flawless simulated player measures an opponent's survival, never its win rate](entries/practices/flawless-player-measures-survival.md) — A perfect controller shuts out every tier, so difficulty has to be read from rally length; win rates need a fallible controller. `game-design`, `testing`, `simulation`
- [Injected aim error does nothing until it exceeds the catcher's own tolerance](entries/practices/injected-error-below-tolerance-is-inert.md) — An error smaller than half the paddle plus the ball radius yields a zero miss rate, so the difficulty dial reads as tuned while rallies never end. `game-design`, `balance`, `simulation`
- [Comparing normals to an axis mis-reports winding; the signed volume of a closed mesh does not](entries/practices/signed-volume-settles-winding.md) — Off-axis parts are correctly oriented while pointing inward, so the heuristic buries the one real inversion in 70% false positives. `3d`, `graphics`, `geometry`
- [A retention window sized for a slow observer keeps stale entities in view when it moves fast](entries/practices/retention-window-scales-with-observer-speed.md) — Nothing was ever freed, so entities sit somewhere they could not have travelled to and it reads as a movement bug. `simulation`, `game-design`, `performance`
- [A bang-bang controller with no deadband drifts toward the faster direction](entries/practices/bangbang-drifts-on-asymmetric-rates.md) — Chatter at the switching point averages the two rates, so a hold-at-a-value loop ends up pinned at one extreme. `control`, `simulation`, `testing`
- [When every simulated skill level scores the same, the fault is the bot's perception](entries/practices/identical-skill-profiles-mean-a-blind-bot.md) — A flawless controller dying as often as a noisy one means the injected error never reaches the outcome; the usual blind spot is a filter that drops the hazard already overlapping the agent. `game-design`, `simulation`, `testing`
- [A moving hazard must commit its path before it enters the player's reaction window](entries/practices/hazards-commit-before-the-reaction-window.md) — Still choosing where to be on the approach makes it unavoidable rather than hard, and halving its speed halves the warning too; lock the final track at a handover distance. `game-design`, `balance`, `simulation`
- [A canvas 2D context silently ignores an invalid value and keeps the previous one](entries/practices/canvas-ignores-invalid-values.md) — A `var()` in `ctx.font`, an incomplete shorthand or a negative `lineWidth` all no-op without throwing, so the drawing uses whatever the last successful call set. `canvas`, `graphics`, `frontend`
- [A minimum-spacing rule sets the output density rather than bounding it](entries/practices/min-gap-sets-density-not-bounds-it.md) — Greedy placement converges on `1/gap` wherever candidates are dense, so the gap replaces the source's own shape; add a rolling-window budget alongside it. `procedural-generation`, `game-design`, `algorithms`
- [AudioContext.currentTime is the time being handed to the output, not the time being heard](entries/practices/audiocontext-currenttime-is-not-audible-time.md) — Visuals driven from it run `outputLatency` ahead of the sound; the offset is per-device, so it never shows on the machine it was built on. `web-audio`, `audio`, `frontend`
- [In a width-scaled projection, camera height must scale with the inverse aspect ratio](entries/practices/camera-height-scales-with-inverse-aspect.md) — A narrow viewport's nearest visible ground collapses, so one huge near polygon fills a third of the screen and reads as a culling bug. `3d`, `camera`, `layout`
- [A rate-limited agent must score where it will be on arrival, not the candidate position itself](entries/practices/score-the-predicted-position-not-the-destination.md) — It commits to gaps it cannot reach, and raising the avoidance penalty pushes harder toward the unreachable one. `game-design`, `control`, `ai`
- [A spacing rule enforced only at spawn time does not hold once the entities move](entries/practices/spawn-time-constraints-do-not-survive-motion.md) — Placement and movement are both correct; the constraint has no authority over the interval between them. `procedural-generation`, `game-design`, `simulation`
- [Redefining grid-template-areas re-points the existing track list at different areas](entries/practices/grid-track-list-is-positional.md) — The sizes stay put while the areas move under them, and any row the list does not cover falls to `grid-auto-rows`; it breaks at one breakpoint only. `css`, `grid`, `layout`
- [A generated-puzzle test that reads the answer key never tests the clue](entries/practices/puzzle-tests-must-parse-the-clue.md) — Applying `puzzle.answer` proves the lock accepts its own key; parse the answer back out of the rendered clue or an unsolvable room passes every test. `testing`, `puzzles`, `game-design`
- [A submit function that stores its own argument overwrites the widget state it was derived from](entries/practices/validator-overwrites-widget-state.md) — Indices in, symbols back out: the rejected attempt looks fine and the control only breaks the next time it is built from state, persisted corruption included. `ui`, `state`, `frontend`

## systems

_No entries yet._

## tools

- [Android Gradle Plugin 9 applies Kotlin itself and fails if you also apply the Kotlin plugin](entries/tools/agp9-applies-kotlin-itself.md) — The standalone `kotlin.android` plugin is now a hard build failure, and `android.kotlinOptions` is gone with it. `android`, `gradle`, `kotlin`
- [gh repo create does not add a git remote unless you pass --source](entries/tools/gh-repo-create-no-remote.md) — The push then fails with a misleading "access rights" error; check `git remote -v` first. `git`, `github`, `cli`
- [A repository's Pages URL is usually absent from its `homepage` field](entries/tools/github-pages-url-not-in-homepage.md) — Enumerate sites by `has_pages` and derive the URL from the repo name; filtering on `homepage` drops most of them with no error. `github`, `github-pages`, `api`
- [Serializing a self-committing workflow does not stop its push being rejected](entries/tools/queued-run-checks-out-a-stale-base.md) — A run pins `github.sha` at creation, so a queued run still checks out the base its sibling pushed past; the retry belongs in the push step. `github-actions`, `ci`, `git`
- [Enabling GitHub Pages from the CLI is an API POST, and its success does not mean the site is live](entries/tools/gh-enable-pages-is-an-api-post.md) — `gh` has no `pages` command; the nested `source` needs bracketed `-f` keys, and the returned URL is live only once `.status` reaches `built`. `github`, `github-pages`, `cli`
- [Two projects served on the same localhost port share one browser cache](entries/tools/localhost-port-shares-one-cache.md) — The browser serves the other project's file at the same URL, so an import fails naming an export the file on disk plainly has. `web`, `caching`, `dev-server`
- [A library's ESM build file can be a façade that imports siblings, so vendoring the named file alone breaks it](entries/tools/vendoring-esm-build-misses-siblings.md) — The vendored file returns 200 and the error names it, while the 404 is a sibling filename that appears nowhere in your source. `javascript`, `esm`, `vendoring`
- [A cache-busting query on a page does not bust the CSS and JS it links](entries/tools/static-server-cache-hides-edits.md) — Dev static servers send no `Cache-Control`, so an edit that "did not apply" is a stale subresource, not a bad edit. `web`, `caching`, `static-sites`
- [GitHub Pages does not render the math that GitHub.com renders in the same file](entries/tools/github-pages-math-needs-mathjax.md) — kramdown rewrites `$$…$$` to `\[…\]` but leaves inline `$…$` as text; MathJax closes both once `$` is an inline delimiter. `github-pages`, `jekyll`, `mathjax`
- [A file named INDEX.md stops jekyll-readme-index from publishing the README](entries/tools/jekyll-readme-index-case-insensitive-index.md) — its "already has an index" test is case-insensitive, so the site root silently 404s; claim `/` with a permalink instead. `github-pages`, `jekyll`, `static-sites`
- [kramdown turns any line containing a pipe into a table; GitHub's renderer does not](entries/tools/kramdown-pipes-become-tables.md) — no delimiter row needed, so prose with `|a|` or `P(A|B)` is silently rebuilt as a table on Pages; `markdown: CommonMarkGhPages` matches GitHub. `github-pages`, `jekyll`, `markdown`
- [A search allowlist that rejects uncrawlable domains fails the whole query](entries/tools/allowed-domains-all-or-nothing.md) — One blocked domain returns 400 and zero results; an errored search is not an empty search. `search`, `web`, `agents`
- [Most `"type": "user"` lines in a Claude Code transcript were not typed by the user](entries/tools/claude-code-transcript-human-prompts.md) — Tool results share the `user` role; filter on `origin.kind == "human"` or overcount prompts ~20x. `claude-code`, `transcripts`, `jsonl`
- [A Claude Code session's subagent transcripts live beside the session file, not inside it](entries/tools/claude-code-subagent-transcripts-are-separate-files.md) — Subagent turns go to `<session-id>/subagents/**/agent-*.jsonl`; parsing only the session file silently drops most of the run. `claude-code`, `transcripts`, `jsonl`
- [Claude Desktop restarts itself to install updates, and only an MDM policy stops it](entries/tools/claude-desktop-stealth-update-restart.md) — An idle timer quits the app; restored sessions lose Remote Control and can fall back to the previous Claude Code build. `disableAutoUpdates` in managed preferences is the sole off switch. `claude-code`, `claude-desktop`, `updates`
- [Claude Code Remote Control can only be switched on from user-level settings](entries/tools/claude-code-remote-control-at-startup.md) — `remoteControlAtStartup` is read from policy/flag/user scope only, and arms a fresh start but not a resume — an app auto-update drops it. `claude-code`, `settings`, `remote-control`
- [yt-dlp re-encodes a file it already produced unless final_ext names the output](entries/tools/yt-dlp-final-ext-enables-skip.md) — The existing-file check maps the download name to the finished one through `final_ext`; without it every run re-downloads and re-encodes, silently. `yt-dlp`, `media`, `python`
- [yt-dlp's ffmpeg_location does not reach its own partial-download check](entries/tools/yt-dlp-ffmpeg-location-misses-path-checks.md) — Section downloads probe `PATH` only and abort with "ffmpeg is not installed" despite a correct flag; put the directory on `PATH`. `yt-dlp`, `media`, `ffmpeg`
- [element.focus() moves activeElement but fires no focus event while the view lacks system focus](entries/tools/focus-events-defer-in-an-unfocused-view.md) — The field accepts text while the focus handler never ran, so automation passes against state the app never set. `testing`, `automation`, `focus`
- [A screenshot can show a frame older than the DOM state you just set](entries/tools/screenshot-lags-committed-dom-state.md) — Hidden and backgrounded surfaces may not paint, so the image shows the pre-change style; assert against getComputedStyle instead. `testing`, `automation`, `browser`
- [setPointerCapture throws for a pointer id the browser has no active pointer for](entries/tools/setpointercapture-throws-for-inactive-pointer-id.md) — A synthetic drag survives only when the id collides with the real mouse pointer's; the throw lands inside the app's handler, half-initialising the gesture. `testing`, `automation`, `pointer-events`
- [Synthetic key events do not trigger the native activation a real key press does](entries/tools/synthetic-key-events-not-real-presses.md) — Untrusted events run no default action and injected ones may arrive with an empty `e.key`; check `isTrusted` before blaming the app. `testing`, `automation`, `keyboard`
- [An automation surface that is hidden delivers no animation frames, so a rAF-driven app cannot be driven through it](entries/tools/hidden-surface-delivers-no-frames.md) — The loop never runs while screenshots still return images; expose a deterministic step instead of waiting on frames. `testing`, `automation`, `browser`
- [A WebGL canvas discards its frame after compositing, so a later read comes back blank](entries/tools/webgl-frame-discarded-after-compositing.md) — the buffer is only readable inside the drawing task; `readPixels` there separates "never rendered" from "never presented". `webgl`, `testing`, `browser`
- [A hidden page stalls asynchronous GPU readback, which silently inverts benchmark results](entries/tools/hidden-page-stalls-gpu-readback.md) — Sync reads stay fast, so one code path looks tens of times slower than a heavier sibling; a duration that ignores input size is measurement, not compute. `testing`, `webgl`, `benchmarking`
- [A capability found in an app bundle belongs to some surface, not necessarily the one in front of you](entries/tools/bundle-capability-is-not-surface-capability.md) — One install carries several renderers; a grep hit is a hypothesis, and what the user sees on screen outranks it. `debugging`, `desktop-apps`, `markdown`
- [A fixed-timestep accumulator and its timestamp baseline are one piece of state](entries/tools/driven-game-loop-needs-accumulator-reset.md) — Resetting one without the other, or feeding a backwards timestamp, drives the accumulator negative and the simulation silently stops while rendering continues. `testing`, `game-loop`, `frontend`
- [A virtual clock has to be the only clock, or what it drives never moves](entries/tools/virtual-clock-must-be-the-only-clock.md) — A step hook plus code still calling performance.now() gives two timelines; the clamp hides it, pinning progress at 0 or snapping it to 1. `testing`, `automation`, `animation`
- [A simulation's scheduled events must use its own accumulated clock, not wall-clock deadlines](entries/tools/sim-deadlines-in-sim-time.md) — Absolute timestamps stop firing the moment anything drives the clock instead of observing it; the continuous state still moves, so it reads as broken event logic. `simulation`, `game-loop`, `testing`
- [An outcome flag read after the round resets reports the reset value, not the outcome](entries/tools/outcome-flag-read-after-reset.md) — Completing an attempt and preparing the next are one transition, so a late-sampled probe returns a flattering, plausible number. `testing`, `instrumentation`, `game-loop`
- [A test harness that steps only the simulation is a different system from the one the frame loop runs](entries/tools/driven-harness-must-drive-the-frame.md) — Camera smoothing and other per-render integrators advance once per batch, so the picture shows a state the app would never reach. `testing`, `game-loop`, `graphics`
- [Stubbing requestAnimationFrame to freeze an app also freezes the surface that captures it](entries/tools/raf-stub-freezes-the-capture.md) — The image goes arbitrarily stale while the DOM and canvas hold the new state; freeze the simulation's rates instead of the frame loop. `testing`, `automation`, `browser`
