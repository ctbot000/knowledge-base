# tools

- [A three.js light's shadow camera ignores the bounds you assign until updateProjectionMatrix is called](shadow-camera-bounds-need-updateprojectionmatrix.md) — LightShadow rebuilds the view matrix every frame but never the projection, so the box stays 10x10 and a normalBias sized for the real one paints spurious shadow. `threejs`, `webgl`, `shadows`
- [Android Gradle Plugin 9 applies Kotlin itself and fails if you also apply the Kotlin plugin](agp9-applies-kotlin-itself.md) — The standalone `kotlin.android` plugin is now a hard build failure, and `android.kotlinOptions` is gone with it. `android`, `gradle`, `kotlin`
- [gh repo create does not add a git remote unless you pass --source](gh-repo-create-no-remote.md) — The push then fails with a misleading "access rights" error; check `git remote -v` first. `git`, `github`, `cli`
- [A repository's Pages URL is usually absent from its `homepage` field](github-pages-url-not-in-homepage.md) — Enumerate sites by `has_pages` and derive the URL from the repo name; filtering on `homepage` drops most of them with no error. `github`, `github-pages`, `api`
- [Serializing a self-committing workflow does not stop its push being rejected](queued-run-checks-out-a-stale-base.md) — A run pins `github.sha` at creation, so a queued run still checks out the base its sibling pushed past; the retry belongs in the push step. `github-actions`, `ci`, `git`
- [Enabling GitHub Pages from the CLI is an API POST, and its success does not mean the site is live](gh-enable-pages-is-an-api-post.md) — `gh` has no `pages` command, the nested `source` needs bracketed `-f` keys, and the `.status` a script waits on is never written for a workflow-built site. `github`, `github-pages`, `cli`
- [Two projects served on the same localhost port share one browser cache](localhost-port-shares-one-cache.md) — The browser serves the other project's file at the same URL, so an import fails naming an export the file on disk plainly has. `web`, `caching`, `dev-server`
- [A library's ESM build file can be a façade that imports siblings, so vendoring the named file alone breaks it](vendoring-esm-build-misses-siblings.md) — The vendored file returns 200 and the error names it, while the 404 is a sibling filename that appears nowhere in your source. `javascript`, `esm`, `vendoring`
- [A cache-busting query on a page does not bust the CSS and JS it links](static-server-cache-hides-edits.md) — Dev static servers send no `Cache-Control`, so an edit that "did not apply" is a stale subresource, not a bad edit. `web`, `caching`, `static-sites`
- [A URL that differs only by its fragment does not reload the page](fragment-navigation-is-not-a-reload.md) — It is a same-document navigation: nothing is fetched and no script re-runs, so an edit-then-navigate loop verifies the previous build and cache headers cannot fix it. `web`, `automation`, `browser`
- [GitHub Pages builds Markdown that has no front matter, and rewrites relative `.md` links](github-pages-builds-bare-markdown.md) — Its default plugins page bare `.md`, index the README and rewrite `.md` links, so a Markdown repo becomes a site with one `_config.yml`; a bare local Jekyll does none of it. `github-pages`, `jekyll`, `static-sites`
- [GitHub Pages does not render the math that GitHub.com renders in the same file](github-pages-math-needs-mathjax.md) — kramdown rewrites `$$…$$` to `\[…\]` but leaves inline `$…$` as text; MathJax closes both once `$` is an inline delimiter. `github-pages`, `jekyll`, `mathjax`
- [A file named INDEX.md stops jekyll-readme-index from publishing the README](jekyll-readme-index-case-insensitive-index.md) — its "already has an index" test is case-insensitive, so the site root silently 404s; claim `/` with a permalink instead. `github-pages`, `jekyll`, `static-sites`
- [kramdown turns any line containing a pipe into a table; GitHub's renderer does not](kramdown-pipes-become-tables.md) — no delimiter row needed, so prose with `|a|` or `P(A|B)` is silently rebuilt as a table on Pages; `markdown: CommonMarkGhPages` matches GitHub. `github-pages`, `jekyll`, `markdown`
- [Markdown strips the backslash from `\{`, `\\` and `\,` inside math, GitHub.com included](markdown-strips-backslash-in-math.md) — Braces vanish, `cases` rows merge and thin spaces print as commas, with no error; write `\lbrace`, `\rbrace`, `\cr`, `\thinspace`. `markdown`, `mathjax`, `github`
- [A search allowlist that rejects uncrawlable domains fails the whole query](allowed-domains-all-or-nothing.md) — One blocked domain returns 400 and zero results; an errored search is not an empty search. `search`, `web`, `agents`
- [Most `"type": "user"` lines in a Claude Code transcript were not typed by the user](claude-code-transcript-human-prompts.md) — Tool results share the `user` role; filter on `origin.kind == "human"` or overcount prompts ~20x. `claude-code`, `transcripts`, `jsonl`
- [A Claude Code session's subagent transcripts live beside the session file, not inside it](claude-code-subagent-transcripts-are-separate-files.md) — Subagent turns go to `<session-id>/subagents/**/agent-*.jsonl`; parsing only the session file silently drops most of the run. `claude-code`, `transcripts`, `jsonl`
- [Claude Desktop restarts itself to install updates, and only an MDM policy stops it](claude-desktop-stealth-update-restart.md) — An idle timer quits the app; restored sessions lose Remote Control and can fall back to the previous Claude Code build. `disableAutoUpdates` in managed preferences is the sole off switch. `claude-code`, `claude-desktop`, `updates`
- [Claude Code Remote Control can only be switched on from user-level settings](claude-code-remote-control-at-startup.md) — `remoteControlAtStartup` is read from policy/flag/user scope only, and arms a fresh start but not a resume — an app auto-update drops it. `claude-code`, `settings`, `remote-control`
- [yt-dlp re-encodes a file it already produced unless final_ext names the output](yt-dlp-final-ext-enables-skip.md) — The existing-file check maps the download name to the finished one through `final_ext`; without it every run re-downloads and re-encodes, silently. `yt-dlp`, `media`, `python`
- [yt-dlp's ffmpeg_location does not reach its own partial-download check](yt-dlp-ffmpeg-location-misses-path-checks.md) — Section downloads probe `PATH` only and abort with "ffmpeg is not installed" despite a correct flag; put the directory on `PATH`. `yt-dlp`, `media`, `ffmpeg`
- [A bare `pip install yt-dlp` lacks mutagen and the YouTube challenge solver](yt-dlp-bare-install-lacks-extras.md) — Every dependency is behind an extra: opus/ogg/flac cover art fails the whole download, and Deno alone cannot solve the JS challenge. Depend on `yt-dlp[default]`. `yt-dlp`, `python`, `packaging`
- [yt-dlp's `--audio-format alac` writes lossy AAC](yt-dlp-alac-writes-aac.md) — ExtractAudio replaces the codec's own ffmpeg options when re-encoding, dropping `-acodec alac`; silent, .m4a either way. Re-pass it via `--ppa`. `yt-dlp`, `ffmpeg`, `audio`
- [yt-dlp's postprocessor "finished" hook sees the file from before the step ran](yt-dlp-finished-hook-sees-pre-step-info.md) — The hook gets the pre-step info copy, so ExtractAudio's "finished" still names the .webm; read outputs, and raise DownloadCancelled, at the next "started". `yt-dlp`, `python`, `hooks`
- [A yt-dlp section download gets the same file name as the whole video](yt-dlp-section-shares-full-filename.md) — The existing-file check then hands a 15-second clip back as the full track, or the reverse; put the range in the file name. `yt-dlp`, `ffmpeg`, `audio`
- [Homebrew's ffmpeg formula has no libvorbis, and ffmpeg-full is keg-only](homebrew-ffmpeg-lacks-libvorbis.md) — The native `vorbis` encoder masks it in `-encoders`; tools asking for `libvorbis` die after downloading, and installing ffmpeg-full changes nothing on `PATH`. `homebrew`, `ffmpeg`, `audio`
- [element.focus() moves activeElement but fires no focus event while the view lacks system focus](focus-events-defer-in-an-unfocused-view.md) — The field accepts text while the focus handler never ran, so automation passes against state the app never set. `testing`, `automation`, `focus`
- [A screenshot can show a frame older than the DOM state you just set](screenshot-lags-committed-dom-state.md) — Hidden and backgrounded surfaces may not paint, so the image shows the pre-change style; assert against getComputedStyle instead. `testing`, `automation`, `browser`
- [setPointerCapture throws for a pointer id the browser has no active pointer for](setpointercapture-throws-for-inactive-pointer-id.md) — A synthetic drag survives only when the id collides with the real mouse pointer's; the throw lands inside the app's handler, half-initialising the gesture. `testing`, `automation`, `pointer-events`
- [Synthetic key events do not trigger the native activation a real key press does](synthetic-key-events-not-real-presses.md) — Untrusted events run no default action and injected ones may arrive with an empty `e.key`; check `isTrusted` before blaming the app. `testing`, `automation`, `keyboard`
- [A screenshot taken right after a state change captures the transition, not the new state](screenshot-catches-the-transition.md) — The DOM is already correct; a cross-fade caught mid-flight shows both states at once and reads as a broken toggle. `testing`, `automation`, `browser`
- [An automation surface that is hidden delivers no animation frames, so a rAF-driven app cannot be driven through it](hidden-surface-delivers-no-frames.md) — The loop never runs while screenshots still return images; expose a deterministic step instead of waiting on frames. `testing`, `automation`, `browser`
- [A WebGL canvas discards its frame after compositing, so a later read comes back blank](webgl-frame-discarded-after-compositing.md) — the buffer is only readable inside the drawing task; `readPixels` there separates "never rendered" from "never presented". `webgl`, `testing`, `browser`
- [A hidden page stalls asynchronous GPU readback, which silently inverts benchmark results](hidden-page-stalls-gpu-readback.md) — Sync reads stay fast, so one code path looks tens of times slower than a heavier sibling; a duration that ignores input size is measurement, not compute. `testing`, `webgl`, `benchmarking`
- [A capability found in an app bundle belongs to some surface, not necessarily the one in front of you](bundle-capability-is-not-surface-capability.md) — One install carries several renderers; a grep hit is a hypothesis, and what the user sees on screen outranks it. `debugging`, `desktop-apps`, `markdown`
- [A fixed-timestep accumulator and its timestamp baseline are one piece of state](driven-game-loop-needs-accumulator-reset.md) — Resetting one without the other, or feeding a backwards timestamp, drives the accumulator negative and the simulation silently stops while rendering continues. `testing`, `game-loop`, `frontend`
- [A virtual clock has to be the only clock, or what it drives never moves](virtual-clock-must-be-the-only-clock.md) — A step hook plus code still calling performance.now() gives two timelines; the clamp hides it, pinning progress at 0 or snapping it to 1. `testing`, `automation`, `animation`
- [A simulation's scheduled events must use its own accumulated clock, not wall-clock deadlines](sim-deadlines-in-sim-time.md) — Absolute timestamps stop firing the moment anything drives the clock instead of observing it; the continuous state still moves, so it reads as broken event logic. `simulation`, `game-loop`, `testing`
- [An outcome flag read after the round resets reports the reset value, not the outcome](outcome-flag-read-after-reset.md) — Completing an attempt and preparing the next are one transition, so a late-sampled probe returns a flattering, plausible number. `testing`, `instrumentation`, `game-loop`
- [A test harness that steps only the simulation is a different system from the one the frame loop runs](driven-harness-must-drive-the-frame.md) — Camera smoothing and other per-render integrators advance once per batch, so the picture shows a state the app would never reach. `testing`, `game-loop`, `graphics`
- [Stubbing requestAnimationFrame to freeze an app also freezes the surface that captures it](raf-stub-freezes-the-capture.md) — The image goes arbitrarily stale while the DOM and canvas hold the new state; freeze the simulation's rates instead of the frame loop. `testing`, `automation`, `browser`
- [A positional argument to `node --test` is a glob pattern, never a directory to search](node-test-positionals-are-globs.md) — `node --test tests/` dies in the module loader on Node 26 and a quoted glob fails on Node 20; bare `node --test` is the portable form. `node`, `testing`, `ci`
- [element.click() proves the handler runs, not that anything can reach the control](element-click-does-not-hit-test.md) — It never hit-tests, so a suite driving a covered or pointer-events-disabled control passes while the feature is dead for everyone. `testing`, `automation`, `dom`
- [javac rejects an unknown -Xlint key outright, so lint flags are not portable across JDKs](xlint-unknown-key-is-an-error.md) — `-this-escape` is JDK 21+, so one hard-coded lint string kills every older-JDK job in the matrix before a line of source is read. `java`, `javac`, `ci`
- [The Tk that ships with macOS is non-functional, not merely old](macos-bundled-tk-is-non-functional.md) — Apple's Tk 8.5.9 composites nothing and never returns from `update()`, so a working paint path looks like a drawing bug; check the patchlevel first. `tkinter`, `macos`, `gui`
- [Chrome writes compiled declarativeNetRequest rulesets into an unpacked extension's own folder](unpacked-extension-writes-ruleset-indexes.md) — `_metadata/generated_indexed_rulesets/` appears on every unpacked load; ignore it in git and exclude it by name from packages. `chrome-extensions`, `git`, `packaging`
- [npm 11 skips dependency install scripts that allowScripts has not approved, and still exits 0](npm-allowscripts-skips-install-scripts.md) — Postinstall downloads (Puppeteer's Chrome) silently don't happen; approvals are version-pinned by default. `npm`, `node`, `ci`
- [A local server standing in for real domains in Chrome tests must also speak HTTPS](test-server-for-real-hosts-needs-https.md) — HSTS preload and HTTPS upgrades send real domains to TLS, so "blocked" assertions pass vacuously; sniff TLS on one port. `testing`, `browser`, `https`
- [Puppeteer's click() never returns for an element in a background tab](puppeteer-click-hangs-in-background-tab.md) — It awaits an IntersectionObserver that a hidden page never fires; bringToFront() first. `testing`, `automation`, `puppeteer`
